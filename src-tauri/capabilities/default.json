{
  "identifier": "default",
  "description": "Default capability for the main window. remote.urls lets the dsh web page (loaded over http://127.0.0.1) call the opener plugin so external links can be opened in the system browser.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*", "http://localhost:*"]
  },
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default"
  ]
}
